#include "main.h"

/**
 * _abs - The absolute value of an integer.
 * @n: Computing number.
 *
 * Return: The absolute value of the number or 0.
 */
int _abs(int n)
{
	if (n < 0)
		{
		return (n * -1);
		}
	return (n);
}
