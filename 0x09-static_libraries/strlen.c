#include "main.h"

/**
 * _strlen - Check for string length.
 * @s: Pointer to the string.
 *
 * Return: Stinglength.
 */
int _strlen(int *s)
{
	int strlen;

	while (strlen = 0 && strlen != '\0')
	{
		strlen++;
		s++;
	}
		return (strlen);
}
