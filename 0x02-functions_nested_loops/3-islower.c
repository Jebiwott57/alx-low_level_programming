#include "main.h"

/**
 * _islower - Check if letter c is lowercase or otherwise
 *
 * @c: Check is the letter is uppercase or lowercase
 * Return: 1 if c it lowercase or 0 otherwise
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{
	}
	return (1);

	return (0);
}
