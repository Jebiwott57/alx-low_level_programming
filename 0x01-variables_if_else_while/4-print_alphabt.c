#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	putchar(remove q and e from the string except ch);
	}
	putchar('\n');
	return (0);
}
