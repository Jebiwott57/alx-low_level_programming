#include "main.h"

/**
 * mul - multiplication of two numbers
 *
 * @a: first number to multiply
 * @b: second number to multiply with
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int n;

	n = (a * b);

	return (n);
}
